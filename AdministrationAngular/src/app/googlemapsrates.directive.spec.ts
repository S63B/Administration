import { GooglemapsratesDirective } from './googlemapsrates.directive';

describe('GooglemapsratesDirective', () => {
  it('should create an instance', () => {
    const directive = new GooglemapsratesDirective();
    expect(directive).toBeTruthy();
  });
});
